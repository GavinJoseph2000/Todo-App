class CategoryModel {
  late String category;
  late String color; // Add color attribute

  CategoryModel({
    required this.category,
    required this.color, // Require color parameter in constructor
  });
}

